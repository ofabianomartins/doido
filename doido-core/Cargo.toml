[package]
name = "doido-core"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
thiserror = "1"
async-trait = "0.1"
tracing = "0.1"
serde = { version = "1", features = ["derive"] }
regex = "1"

[dev-dependencies]
tracing-test = "0.2"
