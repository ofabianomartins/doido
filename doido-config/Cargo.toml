[package]
name = "doido-config"
version = "0.1.0"
edition = "2021"

[dependencies]
doido-core = { path = "../doido-core" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
aes-gcm = "0.10"
base64 = "0.22"
hex = "0.4"

[dev-dependencies]
tempfile = "3"
serial_test = "3"
